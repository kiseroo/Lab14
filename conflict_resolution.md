# Conflict Resolution

## Conflict Details
A merge conflict occurred in `src/main/java/lab14/sict/must/edu/mn/Multiplication.java` when merging `feature/add-multiply-validation` into `develop`. The conflict was in the Javadoc comment of the `multiply` method.

## Resolution Steps
1. Ran `git merge develop` in the feature branch.
2. Identified the conflict in `Multiplication.java`.
3. Edited the file to combine the Javadoc comments from both branches.
4. Ran `git add src/main/java/lab14/sict/must/edu/mn/Multiplication.java`.
5. Ran `git commit` to complete the merge.
6. Pushed the branch and created a PR.
